# ⚙️ Configurations

Agent configuration files organized by type.

- **agent-profiles/** — Agent behavior profile definitions
- **model-configs/** — Model configuration presets
- **scheduler-tasks/** — Scheduled task definitions
